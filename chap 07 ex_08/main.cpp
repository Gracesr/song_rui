a) : cout << alphabet[6] <<endl;
b) : grades [ 4 ] = x;
c) : array <int,5>values = {8,8,8,8,8}
d) :#include <iostream>
    #include <array>
    using namespace std;
    int main ()
    {
      const size_t arraysize = 100;
      array<int,arraysize > temperatures;
      int total;
      for ( size_t i=0;i<temperatures.size();++i)
           total += temperatures[ i ];
    cout <<"Total of array elements is:"<<total<<endl;
    }
e) :
f) :
    const size_t arraysize  = 99;
    array <int ,arraysize >w;
    int smallest,largest;
    cin >> element;
    if ( )
